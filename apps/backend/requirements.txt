fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9
alembic>=1.13
# Not imported anywhere yet — provisioned for the not-yet-built Celery Beat
# scheduler (see README's Known Gaps); app/workers/tasks.py is still a stub.
redis>=5.0
celery>=5.4
langgraph>=0.2
# v3 introduced the OpenTelemetry-based API (get_client, observe,
# propagate_attributes, langfuse.langchain.CallbackHandler); this app is
# written against that surface (currently resolves to v4, the installed
# version — see app/observability/langfuse_client.py).
langfuse>=3.0
# For intent_parser.py's structured-output call — anthropic (Messages API
# output_config.format) and openai (Chat Completions response_format) are
# both wired up; model_provider selects which one runs.
anthropic>=0.40
openai>=1.40
razorpay>=1.4
cryptography>=43.0
python-dotenv>=1.0
httpx>=0.27
pytest>=8.0
pytest-asyncio>=0.24

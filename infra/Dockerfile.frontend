FROM node:20-slim

WORKDIR /app

# Nx workspace: install from the root package.json, then build/run just the frontend project
COPY package.json nx.json tsconfig.base.json ./
RUN npm install

COPY apps/frontend ./apps/frontend
COPY libs ./libs

EXPOSE 3000
CMD ["npx", "nx", "dev", "frontend"]
